import { Routes } from '@angular/router';
import { AddNewStreamComponent } from './addNewStream.component';

export const ADD_STREAM_ROUTES: Routes = [
  {
    path: 'new-stream',
    component: AddNewStreamComponent,
  },
];
